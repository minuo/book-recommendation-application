# 图书推荐应用 📚

<div align="center">
  <p>一个基于Node.js和Express.js开发的图书推荐应用，为用户提供个性化的图书推荐服务</p>
  <a href="#功能特性">功能特性</a> • <a href="#快速开始">快速开始</a> • <a href="#API文档">API文档</a> • <a href="#配置说明">配置说明</a> • <a href="#贡献方法">贡献方法</a>
</div>

## 项目概述

这是一个现代化的图书推荐应用，采用模块化架构设计，提供全面的图书管理和个性化推荐服务。应用支持用户管理、图书搜索、热门推荐、新书推荐和分类浏览等核心功能，并通过RESTful API接口向外部提供服务。

### 核心价值

- **个性化推荐**：基于用户行为和偏好提供精准的图书推荐
- **高效搜索**：支持多种搜索条件和排序方式
- **API友好**：提供清晰的RESTful API接口，方便第三方集成
- **易于扩展**：模块化架构设计，便于添加新功能和集成新的推荐算法

## 技术栈

| 技术 | 用途 | 版本要求 |
|------|------|----------|
| Node.js | 运行环境 | >= 14.0.0 |
| Express.js | Web框架 | ^4.18.0 |
| SQLite | 数据库 (开发/测试) | 内置 |
| Sequelize | ORM | ^6.20.0 |
| JWT | 用户认证 | ^8.5.0 |
| bcryptjs | 密码加密 | ^2.4.0 |
| Jest | 单元测试 | ^28.0.0 |
| Supertest | API测试 | ^6.2.0 |

## 功能特性

### 📦 核心功能

#### 用户管理
- ✅ 用户注册和登录
- ✅ 密码安全加密存储
- ✅ JWT认证机制
- ✅ 用户信息管理

#### 图书管理
- ✅ 图书信息CRUD操作
- ✅ 图书分类管理
- ✅ 图书搜索功能
- ✅ 浏览量统计
- ✅ 图书详情查看

#### 推荐系统
- ✅ 热门图书推荐
- ✅ 新书推荐
- ✅ 分类图书推荐
- ✅ 支持个性化推荐扩展

#### API接口
- ✅ RESTful API设计
- ✅ 统一响应格式
- ✅ 错误处理机制
- ✅ 分页支持
- ✅ 搜索功能

## 快速开始

### 安装步骤

1. **克隆仓库**

```bash
git clone <repository-url>
cd book-recommendation-application
```

2. **安装依赖**

```bash
npm install
```

3. **配置环境变量**

创建 `.env` 文件：

```bash
cp .env.example .env
```

编辑 `.env` 文件，配置必要的环境变量：

```
PORT=3000
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=24h
```

4. **初始化数据库**

```bash
npm run seed
```

5. **启动应用**

```bash
# 开发模式
npm run dev

# 生产模式
npm start
```

应用将在 http://localhost:3000 启动。

## API文档

### API基础路径

所有API接口的基础路径为：`/api/v1`

### 用户接口

| 接口 | 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|------|
| 用户注册 | POST | `/users/register` | 注册新用户 | 不需要 |
| 用户登录 | POST | `/users/login` | 用户登录 | 不需要 |
| 获取用户信息 | GET | `/users/me` | 获取当前用户信息 | 需要 |
| 更新用户信息 | PUT | `/users/me` | 更新用户信息 | 需要 |

### 图书接口

| 接口 | 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|------|
| 搜索图书 | GET | `/books/search` | 搜索图书 | 不需要 |
| 热门图书 | GET | `/books/hot` | 获取热门图书 | 不需要 |
| 新书推荐 | GET | `/books/new` | 获取新书推荐 | 不需要 |
| 图书分类 | GET | `/books/categories` | 获取图书分类 | 不需要 |
| 分类图书 | GET | `/books/category/:category` | 获取分类图书 | 不需要 |
| 图书详情 | GET | `/books/:id` | 获取图书详情 | 不需要 |

### 请求示例

#### 用户注册

```bash
curl -X POST http://localhost:3000/api/v1/users/register \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "email": "test@example.com", "password": "password123"}'
```

#### 搜索图书

```bash
curl "http://localhost:3000/api/v1/books/search?q=JavaScript&page=1&limit=10"
```

### 响应格式

所有API接口都返回统一的响应格式：

```json
{
  "status": "success",
  "message": "操作成功",
  "data": { /* 响应数据 */ },
  "meta": { /* 元数据，如分页信息 */ }
}
```

## 配置说明

### 环境变量

| 变量名 | 描述 | 默认值 | 生产环境建议 |
|--------|------|--------|-------------|
| PORT | 服务器端口 | 3000 | 80或443 |
| DB_NAME | 数据库名称 | book_recommendation.db | 自定义名称 |
| DB_USER | 数据库用户名 | null | 数据库用户名 |
| DB_PASSWORD | 数据库密码 | null | 强密码 |
| DB_HOST | 数据库主机 | null | 数据库服务器地址 |
| DB_DIALECT | 数据库方言 | sqlite | mysql或postgres |
| JWT_SECRET | JWT密钥 | your-secret-key | 随机生成的强密钥 |
| JWT_EXPIRES_IN | JWT过期时间 | 24h | 根据需求调整 |

### 数据库配置

数据库配置位于 `src/config/database.js`，支持以下数据库：

#### SQLite (默认)

```javascript
// src/config/database.js
const sequelize = new Sequelize({
  dialect: 'sqlite',
  storage: process.env.DB_NAME || '../database/book_recommendation.db'
});
```

#### MySQL

```javascript
// src/config/database.js
const sequelize = new Sequelize(
  process.env.DB_NAME,
  process.env.DB_USER,
  process.env.DB_PASSWORD,
  {
    host: process.env.DB_HOST,
    dialect: 'mysql'
  }
);
```

## 项目结构

```
book-recommendation-application/
├── src/
│   ├── app.js                 # 应用入口文件
│   ├── config/
│   │   └── database.js        # 数据库配置
│   ├── middleware/
│   │   └── authenticate.js    # 认证中间件
│   ├── models/                # 数据模型
│   │   ├── User.js            # 用户模型
│   │   ├── Book.js            # 图书模型
│   │   ├── Category.js        # 分类模型
│   │   └── ...                # 其他模型
│   ├── routes/                # API路由
│   │   ├── userRoutes.js      # 用户相关路由
│   │   ├── bookRoutes.js      # 图书相关路由
│   │   └── ...                # 其他路由
│   └── database/              # 数据库文件
│       └── book_recommendation.db
├── test/                      # 测试文件
│   ├── models/                # 模型测试
│   └── routes/                # 路由测试
├── scripts/                   # 脚本文件
│   ├── seed.js                # 数据种子脚本
│   └── reset-database.js      # 数据库重置脚本
├── docs/                      # 项目文档
│   ├── api-design.md          # API设计文档
│   ├── api-documentation.md   # API文档
│   └── technical-implementation-plan.md # 技术实现方案
├── .env.example               # 环境变量示例
├── .gitignore                 # Git忽略文件
├── package.json               # 项目配置
├── jest.config.js             # Jest配置
└── README.md                  # 项目文档
```

## 测试

### 运行测试

```bash
# 运行所有测试
npm test

# 运行特定测试文件
npm test test/models/Book.test.js
npm test test/routes/bookRoutes.test.js

# 运行测试并生成覆盖率报告
npm test -- --coverage
```

### 测试结构

```
test/
├── models/        # 模型单元测试
└── routes/        # API集成测试
```

## 部署

### 开发环境

```bash
npm run dev
```

### 生产环境

1. **构建应用**

```bash
npm run build
```

2. **启动应用**

```bash
npm start
```

### Docker部署 (可选)

```bash
# 构建Docker镜像
docker build -t book-recommendation-app .

# 运行Docker容器
docker run -p 3000:3000 -d book-recommendation-app
```

## 贡献方法

我们欢迎任何形式的贡献！无论是bug报告、功能请求还是代码提交，都可以通过以下方式参与：

### 开发流程

1. **Fork 仓库**

2. **创建特性分支**

```bash
git checkout -b feature/AmazingFeature
```

3. **提交更改**

```bash
git commit -m 'Add some AmazingFeature'
```

4. **推送到分支**

```bash
git push origin feature/AmazingFeature
```

5. **提交Pull Request**

### 编码规范

- 遵循JavaScript标准风格
- 使用ES6+语法
- 为所有新功能编写测试
- 保持代码简洁和可维护性
- 为复杂的代码添加注释

### 提交信息规范

```
<type>: <subject>

<body>

<footer>
```

**Type** 可以是：
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- style: 代码格式调整
- refactor: 代码重构
- test: 测试相关
- chore: 构建或工具相关

## 故障排除

### 常见问题

1. **数据库连接错误**

   - 检查环境变量是否正确配置
   - 确保数据库服务正在运行
   - 检查数据库权限设置

2. **JWT认证失败**

   - 检查JWT_SECRET是否与生成令牌时使用的密钥一致
   - 检查令牌是否过期
   - 检查请求头中的Authorization字段格式是否正确

3. **API响应500错误**

   - 检查服务器日志获取详细错误信息
   - 确保所有依赖都已正确安装
   - 检查数据库表结构是否正确

### 日志查看

```bash
# 查看应用日志
npm run logs

# 查看错误日志
npm run logs:error
```

## 许可证信息

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 联系方式

- **项目维护者**: [Your Name]
- **邮箱**: [your.email@example.com]
- **GitHub**: [minuo/book-recommendation-application](https://github.com/minuo/book-recommendation-application)
- **Issue Tracker**: [https://github.com/minuo/book-recommendation-application/issues](https://github.com/minuo/book-recommendation-application/issues)

## 更新日志

### v1.0.0 (当前版本)

- ✅ 初始化项目结构
- ✅ 实现用户管理功能
- ✅ 实现图书管理功能
- ✅ 实现推荐系统
- ✅ 编写API文档
- ✅ 完成单元测试

## 致谢

感谢所有为这个项目做出贡献的开发者和用户！

---

<div align="center">
  <p>🎉 欢迎使用图书推荐应用！如果有任何问题或建议，欢迎提出 Issue 或 Pull Request。</p>
</div>
